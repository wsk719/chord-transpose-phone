# Implementation Plan — A/C♯、F♯m 偵測不到

更新日期：2026-07-31
測試檔：讚美之泉《我全然獻上》五線譜掃描（1732×2420 JPG）

## 診斷（已用 tesseract CLI 重現，非臆測）

### 根因 1：這張譜被誤判成「文字譜」，整套五線譜前處理沒有啟動
- `isStaff` 判定條件是「dens>0.25 且 run>0.5 的列數 ≥ 10」，此檔只有 **6 列**通過 → `isStaff=false`。
- 原因：掃描件的譜線被 JPEG 雜訊咬斷，最長連續墨水段只到全寬的 0.59（譜線本身也才佔全寬約 0.70），達不到 0.5 的絕對門檻的列數不足。
- 後果：不做譜線遮罩、縮放倍率走文字譜路徑 → 和弦淹沒在五線譜裡，整頁幾乎抓不到東西（實測只剩零星雜訊）。
- **改用五線譜路徑重跑，同一張圖立刻抓到 D、G/D、A、Bm、Em、E7、G/A、D/A、D/F♯… 二十幾個和弦。**

### 根因 2：♯ 記號被 OCR 讀成別的字母
實測 tesseract 對本檔 ♯ 的讀法：

| 譜上 | OCR 實際輸出 | 現況 |
|---|---|---|
| F♯m | `Fem` | 拒絕 |
| D/F♯ | `D/Fi` | 拒絕 |
| A/C♯ | `A/C#` | ✅ 已可解析 |
| A/C♯ | `AICE`、`AICK`、`Alct` | 拒絕 |
| A/C♯ | `AIC` | 解析成 A/C（♯ 整個消失） |

## 解法

### 1. 放寬 `isStaff` 判定（`rowStats` 使用端，約 line 612）
```
舊：dens>0.25 && run>0.5
新：dens>0.25 && (run>0.5 || (run>0.3 && run >= dens*0.75))
```
`run/dens` 是「這列的墨水有多集中在同一段」：真譜線即使被雜訊咬斷，單段仍佔該列墨量 ~0.95；簡譜的節拍底線是很多短段，比值只有 0.1–0.3 → 不會回歸到先前修好的「簡譜被誤判」問題。
本檔套用後通過列數 6 → **23**（門檻 10）。

### 2. 新增 ♯ 誤讀修正 `sharpen()`
- 規則：音名 `[A-G]` 後面接 1–2 個「像♯的雜訊字元」`[eEiIltTkKhHfF4+xX]`，且後面緊接字尾/`/`/m/數字/sus/add/dim/aug/maj → 該段換成 `#`，再丟回 `correctCore` 驗證，能解析才採用。
  - `Fem→F#m`、`D/Fi→D/F#`、`AICE→AIC#→A/C#`、`Alct→Alc#→A/C#`。
- **防誤判（重點）**：`sharpen()` **不進入一般解析路徑**，只在兩個已經確定安全的位置當「救援」用：
  1. `detectFrom` 內既有的救援區塊（該行已確認 ≥2–3 個和弦才啟動，並沿用既有字高比對 `h<=lh*1.6`）。
  2. `sparseCorrect`（第三輪稀疏掃描，本來就限定在和弦列的高度帶內）。
  - 這樣歌詞行的 `Get`、`Fit`、`Bee`、`Gem` 這類英文字不會被變成 `G#`、`F#`、`B#`、`G#m`（歌詞行湊不到 2 個和弦，救援根本不會啟動）。

### 3. 不做的事
- 不改 OCR 引擎、不加外部套件、不動轉調與標註邏輯。
- `AIC`（♯ 完全沒被讀出來）不硬猜成 A/C♯ —— 沒有證據就不編造；同位置另一輪的 `AICE` 會在合併時勝出（文字較長者優先），實務上會被救回。

## 驗證方式
1. 用 tesseract CLI 重現前處理（含遮罩）產生 TSV，node 直接跑 HTML 內的解析邏輯，比對修改前後在本檔上的和弦數與內容。
2. 回歸：`Get / Fit / Bee / Gem / Be / Em / Fm / Am` 等字串不得被 `correctToken` 判成和弦。
3. `node --check` 語法驗證。
4. 更新 `./project_knowledge.md` 後 git commit。
