# Implementation Plan — 手動標註框拖曳與字級調整

更新日期：2026-07-31

## 需求歸納

1. 手動標註的方框（綠框）要可以**拖曳移動**位置。
2. 標註的**字體大小可調整**。
3. 調整後的標註要能**覆蓋原圖上的和弦**（覆蓋範圍要跟著變大）。

## 解法設計（皆在 和弦轉調工具.html 內，不引入任何新套件）

### 1. 拖曳移動（綠框）
- 將 `canvas.onclick` 改為 `pointerdown / pointermove / pointerup` 事件系統。
- 位移 ≤ 3px 視為「點擊」→ 完全保留原行為（點藍框排除、點綠框刪除、點空白補和弦）。
- 位移 > 3px 且按在綠框上 → 拖曳移動該框。
- 滑鼠移到框上會顯示對應游標（move / resize / pointer）。

### 2. 大小調整（綠框，每框獨立）
- 綠框右下角新增**縮放手把**（小綠方塊），拖曳手把＝等比例縮放整個框。
- 標註字級隨框高度縮放；**覆蓋範圍＝框的範圍**，框拉大即可蓋掉原圖上更大的和弦。

### 3. 全域字級調整（所有標註）
- 控制列新增「標註字級」滑桿（50%–300%，預設 100%），同時縮放 OCR 框與綠框的標註字體。

### 4. 說明文字
- 更新畫面上的操作提示：拖曳綠框＝移動、拖右下角手把＝縮放。

## 完成後
- 語法驗證（node --check 抽出的 script）。
- 更新 ./project_knowledge.md（知識沉澱）。
- 專案目前**尚未有 git 倉庫**：將執行 `git init` 後首次提交（含 .gitignore 略過暫存檔）。

## 不做的事
- 不改動 OCR、轉調核心邏輯。
- 不引入外部套件。
